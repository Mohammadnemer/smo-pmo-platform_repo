# Session D1

## Session ID

D1

## Scope

Create the initial database foundation for the platform by adding a DbContext, a base entity with tenant_id and audit fields, and the first migration for tenants, org units, users, and roles.

## Done when

The migration applies and every table carries tenant_id.

## Prerequisites

- .NET 10 SDK installed and available in the environment.
- The repository solution builds successfully.

## Decisions made

- Added a dedicated Platform EF Core DbContext for the initial platform schema.
- Used a shared base entity to carry tenant ownership and audit metadata across the first set of entities.
- Registered the DbContext in the Platform module and ensured the API startup creates the database schema.

## Files touched

- src/Platform/PlatformDbContext.cs
- src/Platform/Platform.csproj
- src/Platform/Migrations/20260730000000_InitialPlatformSchema.cs
- src/Platform/Migrations/PlatformDbContextModelSnapshot.cs
- src/Api/Program.cs

## How it was verified

- Ran `dotnet build` successfully after the implementation.
- Confirmed the API project compiles with the new DbContext registration and migration files in place.

## Follow-ups

- Wire a real PostgreSQL connection string and environment-based configuration.
- Add the tenant-aware RLS policy work in a later session.
