# /infra — Azure infrastructure (Bicep)

Intentionally empty in **T1**. Azure resources (Container App environment,
PostgreSQL Flexible Server, Blob, Key Vault, Application Insights in the GCC
region) are authored as Bicep in build session **T2 · Azure resources**.
