<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <RootNamespace>SmoPmo.Worker</RootNamespace>
    <AssemblyName>SmoPmo.Worker</AssemblyName>
  </PropertyGroup>
  <ItemGroup>
    <FrameworkReference Include="Microsoft.AspNetCore.App" />
  </ItemGroup>
  <ItemGroup>
    <!-- Worker reacts to domain events via IMessageBus in /Shared; it needs no module. -->
    <ProjectReference Include="..\Shared\Shared.csproj" />
  </ItemGroup>
</Project>
